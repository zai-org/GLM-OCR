# Config Templates

Use these files as starting points for the two most common SDK setups:

- `config.maas.example.yaml`: hosted MaaS API flow
- `config.selfhosted.example.yaml`: local vLLM / SGLang / compatible server flow

Copy the template that matches your deployment path, then fill in the API key, host, port, and model name for your environment.
